# Nil and truthy values in elixir
# Must read the nil and truthy doc of the book ok.

# Nil and false are the falsy values except everything is a truthy value in elixir.
import IO

puts(:nil) # Returns nothing cause it holds nothing.
# we can use the nil value as without the colon too
puts(nil == :nil)

